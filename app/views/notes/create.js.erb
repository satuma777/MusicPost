function check(content){
    if(content = ""){
        if(window.confirm('音楽の説明文が書かれていませんが、よろしいですか？')){ // 確認ダイアログを表示
            return true; // 「OK」時は送信を実行
        }
        else{ // 「キャンセル」時の処理
             return false; // 送信を中止
        }
    }
}