 <%= form_for(@sound) do |f| %>
<!--↑form_tag sound_path(@sound.id)と同じ。method（Verb）は、id（@sound.id）を引数に取る（この時、Verbはpatchとなるので、updateアクションに飛ぶ）か取らない（この時は、post、createアクションに飛ぶ）かはすでに@sound.idがあるかどうかで自動的に判断してくれる。-->
    <% if @sound.errors.any? %>
        <div class="alert alert-danger">
            <p><%= @sound.errors.count %>件のエラーがあります。</p>

            <ul>
                <% @sound.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
                <% end %>
            </ul>
        </div>
    <% end %>

    <br>

    <p>
    <div>
     タイトル
     </div>
    <%= f.text_field :title, placeholder: "タイトルを入力してください。", class: "form-control title-field"%>
    <!--クラスは、=ではなく、:（ハッシュ）で指定する。-->
    </p>
    <p>
    <div>
    音楽の説明
    </div>
    <%= f.text_area :content, maxlength:1000, placeholder: "音楽の説明を入力してください。", class: "form-control content-field" %>
<%
=begin
%>
    ↑maxlengthにおいて、全角文字は2文字と数えられる。
<%
=end
%>
    <div>
    <lavel class: "control-label"><%= @thumb_value %>
    </div>
        <%= f.file_field :image, size: 50 , class: "form-control  image-field" %>
    </lavel>
    </p>

    <p>
    <%= f.submit "アップロード" %>
    </p>

<% end %>